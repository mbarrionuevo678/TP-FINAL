# Terraform files
*.tfstate filter=lfs diff=lfs merge=lfs -text
*.tfstate.backup filter=lfs diff=lfs merge=lfs -text
# Crash log files
crash.log
# Exclude .terraform directory
.terraform/
# Exclude .terraform.lock.hcl file
.terraform.lock.hcl
# Exclude local environment override files
terraform.tfvars
*.tfvars.json
# Exclude sensitive files (make sure to add your own)
*.pem
*.key
*.p12
*.cer
*.csr
*.crt
*.der
# Exclude plan files
*.plan filter=lfs diff=lfs merge=lfs -text
# Exclude other potentially sensitive information
*.kubeconfig
*.conf
.terraform/* filter=lfs diff=lfs merge=lfs -text
